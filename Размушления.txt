sudo chown -R $USER:$USER .
sudo chmod -R 777 . права на перезапись, чтобы не возмущялось
Я НЕНАВИЖУ ЭБУЧИЙ ФРОНТ. КНОПКИ РИСОВАТЬ БЫЛО ВЕСЕЛЕЕ, ЧЕМ ЭТО
docker-compose exec php php yii migrate - миграция в корневой папке, это чтобы восстановить бд

http://localhost:8080/index.php?r=site/storage
http://localhost:8080/index.php?r=site/login
http://localhost:8080/index.php?r=site/logout - это пост запрос, чтобы разлогиниться, пока нет норм кнопки на фронте

ПОФИКСИТЬ КАК ВЫГЛЯДИТ ЭКРАН, КОГДА ПЫТАЕШЬСЯ ЕГО УВЕЛИЧИТЬ. НЕ ЗАБЫТЬ!!!
/* Лист всегда будет занимать 30% от ширины окна */ - привязать всё остальное к ширине экрана!!!
КРАСИВЫЕ ССЫКЛИ - sudo nano /etc/hosts можно сделать это через команду, но я не уверена нужно ли это.

Что если пользователь сделает 2 файла с одинаковым именем? - ПОФИКСИТЬ

http://localhost:8080/file/index - чек джейсончики
ЗАПОМНИ ЭТО УЖЕ БЛТЬ git push -u origin main
API через Axios CSRF - ЗАЩИта

ЧТО МНЕ ОСТАЛОСЬ:

ТЕСТЫ
масштабирование - в пизду я хз как это делать
фикс если пароли не совпадают ГОТОВО
лист главный экран опустить чуть ниже ГОТОВО
БД access token - нужен ли? ПОД ВОПРОСОМ

БАГИ
файл - дт с 1с не азгружается. ФИКС
файл pdf - не загружается ФИКС
файл mp4 не загружается ФИКС НО НЕ ОЧЕНЬ БОЛЬШИЕ ФАЙЛЫ 
название старого файла не стирается, ког8да файл загружается
Интерфейс слетает при слишком длинномс названии файла и jpg не грузит ФИКС
pptx  не грузит ФИКС
странно какие то файлы pdf загружвет какие то нет скорее всего размер ФИКС
полетела рега ФИКС
пптх при загрузке файла обрезает имя почему-то ФИКС


ГЛАЗИК ФИКС
Два раза рега. после регистрации перекидывает на вход, но потом невозможно войти ФИКС
Плашка неправильный пароль не выскакивает ФИКС


ТЕСТ 
ЗАПУСК docker-compose exec php vendor/bin/codecept run functional LoginFormCest - имя файла в конце если надо
СБРОСИТЬ КЭШ ТЕСТОВ docker-compose exec php vendor/bin/codecept build
РУГАЕТСЯ НА ПРАВА docker-compose exec php chmod -R 777 /var/www/html/web/uploads